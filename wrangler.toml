name = "landlogistik"
main = "worker.js"
compatibility_date = "2024-12-18"
compatibility_flags = ["nodejs_compat"]
workers_dev = true

[assets]
directory = "./dist"
binding = "ASSETS"

[vars]
CONTACT_FROM = "kontakt@landlogistik.at"
CONTACT_TO = "office@landlogistik.at"
# Secrets (never commit): wrangler secret put RESEND_API_KEY / TURNSTILE_SECRET
# TURNSTILE_SITE_KEY → CF dashboard Build variables
